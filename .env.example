# Vercel build-time configuration
NEXT_PUBLIC_API_BASE_URL=https://your-api-domain.up.railway.app/api/v1

# Railway runtime configuration; use the exact Vercel production origin only
ALLOWED_ORIGINS=https://your-web-domain.vercel.app
